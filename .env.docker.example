# Example env for Docker Compose (copy to .env and fill keys)
# Get keys from provider dashboards — never commit real values.

DEEPSEEK_API_KEY=
GROQ_API_KEY=
OPENAI_API_KEY=
NVIDIA_API_KEY=
PORT=3000
